<?php
defined('REDOME_CONTACT_BOOT') || die('Direct access forbidden');
/**
 * Override `config.php`. Zkopíruj na `config.local.php` (bez `.example`),
 * doplň secret. Na produkci patří do `subdom/<branch>/api/`, kde ho přes
 * deploy zachová `cli/deploy.sh` krok 1b.
 */
return [
    'hcaptcha' => [
        'secret' => 'ES_…',
    ],
];
